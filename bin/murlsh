#!/usr/bin/env ruby

require 'fileutils'

FileUtils.cp_r(
  %w{.htaccess config.ru config.yaml public/ Rakefile}.collect { |x| 
    File.join(File.dirname(__FILE__), '..', x) }, '.', :verbose => true)

puts <<eos
Next steps:

edit config.yaml
rake db:init user:add
eos
